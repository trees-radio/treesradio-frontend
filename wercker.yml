#
# Config file for Wercker Continuous Integration
#

box: node
# Build definition
build:
  # The steps that will be executed on build
  steps:
    # A custom script step, name value is used in the UI
    # and the code value contains the command that get executed
    - script:
        name: echo nodejs information
        code: |
          echo "node version $(node -v) running"
          echo "npm version $(npm -v) running"
    # A step that executes `npm install` command
    - npm-install

    - script:
        name: npm run lint
        code: |
          npm run lint
    # A step that executes `npm test` command
    #- npm-test

    ###
    #
    - script:
        name: npm run build-prod
        code: |
          npm run build-prod
  after-steps:
    - wercker/slack-notifier:
        url: $SLACK_WEBHOOK_URL
        username: wercker

# Deployment definition
deploy:

  steps:
    - script:
        name: no deploy selected
        code: |
          echo "no deploy selected"
  Dev:
    - script:
        name: npm run build-prod
        code: |
          npm run build-prod
    - zbuttram/wercker-firebase-deploy@0.1.2:
        firebase: "treesradio-dev"
        token: "-K1gUPDLDY1-gqa_SUNN|0ff924bf01360975d71d3ddcf7328193"

  after-steps:
    - wercker/slack-notifier:
        url: $SLACK_WEBHOOK_URL
        username: wercker
